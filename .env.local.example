# Copy to .env.local (gitignored). A personal Sketchfab API token lets
# scripts/assets/fetch.mjs pull the approved CC BY models through the
# download API instead of a browser download. Sketchfab → Settings →
# Password & API → API token.
SKETCHFAB_TOKEN=
