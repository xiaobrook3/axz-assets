# approved originals, re-fetchable from manifests/
raw/
node_modules/
.DS_Store
.vercel/
# converted outputs, regenerable from raw/ + axz-wab-source/scripts/assets
derived/
# written by the Vercel CLI on link; holds a token
.env.local
